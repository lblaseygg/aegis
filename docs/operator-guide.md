# Operator Guide

Core flows:

- `aegis doctor`: verify local services and config.
- `aegis models list`: inspect locally served Ollama models.
- `aegis rag ingest <path>`: load local documents into the persistent vector store.
- `aegis rag query "<question>"`: run grounded retrieval and answer generation.
- `aegis chat`: launch the interactive Ink shell.
